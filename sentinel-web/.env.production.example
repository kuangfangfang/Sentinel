# Copy to .env.production before `npm run build` for a hosted deployment.
# Vite bakes these values into the static bundle and generated SEO files at build time.

VITE_API_BASE_URL=/api
VITE_SITE_URL=http://3.104.237.26
